spring:
  application:
    name: eureka
  cloud:
    config:
      uri: http://config:8888
      fail-fast: false
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false